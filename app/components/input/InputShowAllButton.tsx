import { TextField } from "@shopify/polaris";

export const ShowAllButton = () => {
    return <TextField label="Show all button name" autoComplete="off" placeholder="Show all" helpText="It is displayed when the initial display lines of the table of contents is set." />;
};