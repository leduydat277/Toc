import { TextField } from "@shopify/polaris";

export const InputHideBtn = () => {
    return <TextField label="Hide button name" autoComplete="off" placeholder="Hide" helpText="It is displayed when the table of contents is open." />;
};