import { TextField } from "@shopify/polaris";


export const InputShowBtn = () => {
    return <TextField label="Show button name" autoComplete="off" placeholder="Show" helpText='It is displayed when the table of contents is close.' />;
};